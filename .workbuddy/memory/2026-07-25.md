# 2026-07-25

- 完善解析文本模板占位符机制：
  - `core/sender.py` 的 `render_template` 现在会检测模板中使用了但解析结果未提供的占位符，并通过 `logger.warning` 输出黄色警告。
  - 使用 `string.Formatter().parse` 正确识别占位符，避免误匹配 `{{ }}` 转义。
  - 未提供的占位符自动隐藏为空字符串（而非显示 `-`）。
- 统一各平台统计占位符：
  - B站视频 extra 同时返回 `reply` 与 `comment`（值相同）。
  - 微信视频号解析结果补充 `like/favorite/comment/share` 统一占位符，保留原有 `info` 文本。
  - 抖音、小红书、微博、快手已有对应统计字段，保持现状。
- 更新 `_conf_schema.json` 中「解析文本模板」的 hint，完整列出通用占位符及各平台支持的统计占位符。
- 修复 `_conf_schema.json` 中 `detect_action` 配置项使用 AstrBot 不支持的 `select` 类型导致插件载入失败的问题，改为 `string` 类型。
- 修复 `core/clean.py` 中 `clean_cron` 为空字符串时 `CronTrigger.from_crontab` 报错的问题，空值时跳过自动清理任务。
- 版本提升至 `v1.1.0`，并更新 `CHANGELOG.md`。
- 已通过 `py_compile` 与 JSON 语法检查。
